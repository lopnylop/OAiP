for (int i = 0; i < 10; i++)
{
    cout << "b[" << i << "] = ";
    // Введение значение записываем в i-й элемент массива b
    cin >> *(b + i);
}
...
for (int i = 0; i < 10; i++)
// выводим на экран значение i-го элемента массива b
cout << "b[" << i << "] = " << *(b+i) << endl;

// объявили динамический массив для хранения 10 целых чисел
int *b = new int[10];
// ввод элементов массива
